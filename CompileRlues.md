---
alwaysApply: true
notes:
若在每次对话时问题需触发此规则，请将上方的“alwaysApply: false”中的false改为true，如本规则不需要跟随项目进行commit，请在.gitignore文件中添加该规则文件。

---

## 步骤一：识别模块类型

### 🚀 可运行服务模块（编译目标）
**特征：**
- 包含 SpringBoot 启动类 (`@SpringBootApplication`)
- pom.xml 中有 `spring-boot-maven-plugin`
- 命名通常为：`*-webservice`、`*-service`、`*-application`、`*-server`

### 🧩 依赖子模块（不可独立编译）
**常见后缀（包括但不限于）：**
- `-api`: 接口定义
- `-runner`: 运行时模块
- `-component`: 组件模块
- `-domain`: 领域模型
- `-infra`: 基础设施
- `-sdk`: SDK模块
- `-common`: 公共工具
- `-core`: 核心逻辑
- `-dao` / `-mapper`: 数据访问
- `-model`: 数据模型
- `-client`: 客户端
- `-biz`: 业务逻辑
- `-facade`: 外观接口
- `-starter`: 启动器

**判断依据：** 如果模块名有上述后缀或类似特征，且无启动类，则为依赖子模块

**检查项：**
- [ ] 已识别修改的模块类型
- [ ] 已确认该模块所属的父模块或根目录

-----

## 步骤二：确定编译位置

### 决策规则
```text
修改了什么模块?
│
├── 依赖子模块 (-api, -runner等)
│   └─ 找到父级可运行服务模块目录
│      └─ 在父模块目录执行编译
│
├── 可运行服务模块
│   └─ 推荐在项目根目录编译
│
└─ 不确定或修改多个模块
    └─ 在项目根目录编译（最安全）
```

### 快速定位父模块

**方法1：查看 pom.xml 中的 parent**
```bash
cat pom.xml | grep -A 3 "<parent>"
# 输出的 <artifactId> 就是父模块名
```

**方法2：查看上级目录**
```bash
ls ../
# 如果看到多个同名前缀的子模块，说明上级是父模块
```

**方法3：找项目根目录**
```bash
# 使用 git
git rev-parse --show-toplevel

# 或手动向上查找
while [ ! -f ../pom.xml ]; do cd ..; done && pwd
```

**检查项：**
- [ ] 已确定编译执行目录（父模块或根目录）

-----

## 步骤三：执行编译

### 标准命令
```bash
mvn clean -U install package -Dmaven.test.skip=true
```

### 推荐策略

#### ⭐ 策略1：根目录编译（最推荐）
```bash
# 跳转到项目根目录
cd /path/to/project-root

# 执行编译
mvn clean -U install package -Dmaven.test.skip=true
```
**适用：** 所有场景，最安全

#### 策略2：父模块编译
```bash
# 跳转到父模块（例如：xxx-webservice），注：下方为示例，具体由当前工程决定
cd oversea-zt-platform-webservice

# 执行编译
mvn clean -U install package -Dmaven.test.skip=true
```
**适用：** 仅修改了某个服务下的子模块

#### ❌ 禁止：子模块独立编译
```bash
# ❌ 错误示例
cd oversea-zt-platform-webservice-api
mvn clean -U install package -Dmaven.test.skip=true
# 结果：编译失败，缺少依赖
```

**检查项：**
- [ ] 在正确的目录执行编译
- [ ] 使用完整的标准命令
- [ ] 不在依赖子模块目录编译

-----

## 步骤四：验证结果

### 成功标志
```text
[INFO] BUILD SUCCESS
[INFO] Total time: XX:XX min
```

### 验证检查
```bash
# 检查构建产物
ls -lh target/*.jar

# 检查本地仓库
ls -lt ~/.m2/repository/com/your-group/your-artifact/
```

**检查项：**
- [ ] 看到 `BUILD SUCCESS`
- [ ] jar/war 文件已生成
- [ ] 无 ERROR 级别错误

-----

## 常见问题处理

### 问题1：子模块编译失败

[ERROR] Could not resolve dependencies

**解决：** 返回父模块或根目录编译
```bash
cd .. # 返回上级
mvn clean -U install package -Dmaven.test.skip=true
```

### 问题2：依赖冲突

[ERROR] package xxx does not exist

**解决：** 清理并强制更新
```bash
cd /path/to/project-root
mvn clean -U install package -Dmaven.test.skip=true
```

### 问题3：找不到根目录
```bash
# Git 项目
git rev-parse --show-toplevel

# 非 Git 项目：向上查找直到没有 pom.xml
while [ -f ../pom.xml ]; do cd ..; done && pwd
```

-----

## 整体检查清单

### 核心原则
- [ ] ❌ **禁止直接编译子模块** (-api、-runner、-component等)
- [ ] ✅ **默认在项目根目录编译**
- [ ] ✅ **使用标准命令**：`mvn clean -U install package -Dmaven.test.skip=true`

### 执行步骤
- [ ] 识别模块类型（子模块 or 服务模块）
- [ ] 确定编译位置（父模块 or 根目录）
- [ ] 在正确目录执行标准编译命令
- [ ] 验证编译成功及产物生成

-----

## 快速示例

### 场景1：修改了 API 模块
```bash
# 当前位置：xxx-webservice/xxx-webservice-api

# ❌ 错误
mvn clean -U install package -Dmaven.test.skip=true

# ✅ 正确：返回父模块
cd ..
mvn clean -U install package -Dmaven.test.skip=true

# ★ 最佳：返回根目录
cd ../../
mvn clean -U install package -Dmaven.test.skip=true
```

### 场景2：修改了多个模块
```bash
# ★ 唯一正确做法：根目录编译
cd /path/to/project-root
mvn clean -U install package -Dmaven.test.skip=true
```

### 场景3：不确定情况
```bash
# 默认策略：根目录编译（最安全）
cd /path/to/project-root
mvn clean -U install package -Dmaven.test.skip=true
```

-----

## 总结

1. **识别后缀**：子模块后缀（-api/-runner/-component/-domain/-infra/-sdk等）
2. **禁止独立**：不要在子模块目录直接编译
3. **推荐根**：在项目根目录执行编译
4. **命令全**：`mvn clean -U install package -Dmaven.test.skip=true`
5. **原则**：不确定时就在根目录编译